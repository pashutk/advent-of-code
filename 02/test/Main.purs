module Test.Main where


import Prelude

import Data.List.NonEmpty (singleton)
import Effect (Effect)
import Effect.Class.Console (log)
-- import Main (digitsToInt)

-- main :: Effect Unit
-- main = do
--   log (show (digitsToInt 10 (singleton 3)))
